<?xml version="1.0" encoding="utf-8"?>
<annotations version="1.0">
	<metadata 
		name="Invalidate" 
		description="Marks and tracks property changes to trigger 'IInvalidating' invalidations.">
		
		<context name="variable" />
		<context name="getter" />
		<context name="setter" />

		<attribute 
			description="Denotes the invalidation phases that should be invalidated when the specified variable or property changes." 
			type="String"
			values="displaylist,properties,size"
			defaultValue="properties"
			required="false" />
		
	</metadata>
	
	<metadata
		name="Persistent"
		description="Marks a property as eligible to be persisted by a persistent data store.">
		
		<context name="variable" />
		<context name="getter" />
		<context name="setter" />
		
	</metadata>
</annotations>